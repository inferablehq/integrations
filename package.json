{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "inferable": "^0.10.0",
    "tsx": "^4.16.5"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  },
  "scripts": {
    "update": "npm i inferable@latest && lerna exec -- npm i inferable@latest",
    "audit": "lerna exec -- npm audit"
  }
}